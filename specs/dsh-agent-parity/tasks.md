# DSH 简历 Agent 能力对齐：任务

- [x] 1. 引入 CVAgent 本地简历生产契约
  - 迁入岗位定向、证据、压缩 STAR、页数和模板策略。
  - _需求：1、2_

- [x] 2. 补齐只读 Agent 工具
  - 增加 `icon_list` 和 `layout_validate`，限制返回体并补测试。
  - _需求：3、5_

- [x] 3. 实现受限版式调优建议
  - 增加只提案、不持久化的 `presentation_suggest`；确认后的写入仍由 `presentation_update` 完成。
  - _需求：2、4_

- [x] 4. 扩展岗位化投递版本元数据
  - 保存可选岗位、公司和 JD 路径并保持旧版本可读。
  - _需求：6_

- [x] 5. 验收
  - 后端 45/45、React/Vite 生产构建、真实浏览器的 Agent 只读工具闭环均通过；更新对接复盘。
  - _需求：1–6_
