# CVAgent 工程协作规则

## 运行时边界

- 唯一 Web 入口是 `frontend/react/`；`frontend/server.mjs` 的根路径只负责重定向到 `/react/`。
- `frontend/server.mjs` 只提供 React 构建产物和 `/api/*` 代理；`backend/src/server.js` 只提供 API、Agent、会话、模板与渲染能力，不再提供页面副本。
- `frontend/react/src/runtime/` 是当前仍使用 DOM 适配层的共享运行时边界。新增功能不得再在仓库根目录复制一套聊天、SSE 或工作台实现；迁移完成后再按模块删除运行时文件。
- `frontend/react/src/react-pane-bridge.tsx` 只负责把当前工作台 pane 接到 React；不在业务模块中再创建第二套全局状态。

## 不可破坏的契约

- Agent 事件必须带 `sessionId`，运行事件要能关联 `runId`，工具事件要能关联 `toolCallId`，渲染验收要能关联 `renderId`。
- 流式链路必须覆盖订阅竞态、重连、刷新恢复、完成落库和错误终态；最终消息不能依赖某个刚好在线的浏览器连接。
- 简历写入、渲染、真实 A4 测量、保存正式版本仍由后端工作流和工具负责；前端不伪造工具结果、思考内容或验收结论。
- Agent 只读取公开的生产 Skill 和工具契约，不输出私有链式推理原文；面向用户的状态必须是可验证的工具/结果摘要。

## 完成标准

每次跨前后端改动都必须依次完成：

1. 更新相关测试和对接复盘文档；
2. 在 `backend/` 执行 `npm.cmd test`；
3. 在 `frontend/react/` 执行 `npm.cmd run build`；
4. 启动 3180/3191 后，在 `http://127.0.0.1:3191/react/` 真实刷新、发送、观察 SSE 和工具状态；
5. 检查 `git diff --check`、`git status`，确认没有日志、密钥、构建产物或旧入口混入提交。

## 变更纪律

- 先查找调用方和测试，再删除文件；删除旧实现时必须同步迁移唯一调用方、契约测试和文档。
- 规则只记录不明显但必须保持的边界；机械格式、类型和覆盖率检查交给脚本与 CI。
- 发现回退时优先定位单一运行时来源、事件时序和状态持久化，不通过再叠加一个兼容补丁掩盖重复实现。
