# Agent 聊天链路与功能联调任务

- [x] 1. 建立空工作区的新用户入门联调。
  - 使用独立临时 registry、空文件夹与无历史浏览器状态。
  - 断言 `resume.md` 只会在用户进入/选择空工作区时由系统初始化生成。
  - _Requirement: 1, 7_

- [x] 2. 建立脚本化 Agent 回放与 API/SSE 回归用例。
  - 覆盖成功闭环、测量阻塞和 stale renderId。
  - _Requirement: 2, 3, 4_

- [x] 3. 让浏览器验收能使用受控测试工作区并保留脱敏证据。
  - 验证聊天时间线、工具组展开/收起、刷新恢复。
  - _Requirement: 5, 7_

- [x] 4. 执行两种桌面视口的工作台截图验收。
  - 验证三栏不重叠、A4 完整可见、控制台无新增错误。
  - _Requirement: 6, 7_

- [x] 5. 将已发现问题转为回归测试并修复。
  - 优先处理 `measurement requires a current render` 的可复现情况。
  - _Requirement: 2, 3, 4, 7_

- [ ] 6. 完成真实模型的人工烟测。
  - 仅在用户明确选择测试材料和模型服务后执行；不纳入自动测试。
  - _Requirement: 7_

已发现并修复：原生 `prompt()` 阻塞空工作区创建、React A4 pane 挂载竞态、首次空白草稿可绕过保存门、验收类事件未推送到 SSE。
